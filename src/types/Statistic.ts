export type Statistic = {
    total_agents: number
    total_users: number
    total_conversations: number
    total_messages: number
    total_agents_with_recent_iteractions: number
    total_users_with_recent_iteractions: number
    most_active_agent_id: number
    most_active_agent_name: string
    message: null
}
